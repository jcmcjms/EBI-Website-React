/**
 * Vitest setup — runs once before the test suite.
 *
 * We only need this for any future global config; currently a no-op.
 * The interesting test infra (server-only stub) lives in
 * vitest.config.mts's `resolve.alias`.
 */
export {};